<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Start-Up App" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Start-up App" %>">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= yield(:head) %>
</head>
<body class="<%= controller_name %> <%= action_name %>">
<header class="navbar navbar-fixed-top">
  <nav class="navbar-inner">
    <div class="container">
      <%= render 'layouts/navigation' %>
    </div>
  </nav>
</header>
<div id="main" role="main">
  <div class="container">
    <% if current_page?(root_url) %>
        <div class="row">
          <div class="span12">
            <%= render 'layouts/messages' %>
          </div>
        </div>
        <div class="hero-unit">
          <h1>Start-up app</h1>
          <p> I see, you've found your way </p>
          <p> Do report any issues that you see</p>
          <a href="#" class="btn btn-large btn-success">Get Started!!!</a>
        </div>
    <% else %>
        <div class="content">
          <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
        </div>
    <% end %>
  </div>
  <!--! end of .container -->
</div>
<!--! end of #main -->
</body>
<footer>
  <%= render 'layouts/navigation_footer' %>
</footer>
</html>
